# AWS EC2 콘솔 접속 방법

## 🌐 1. AWS 콘솔 접속

### 방법 1: 직접 링크 (가장 빠름)

```
https://ap-northeast-2.console.aws.amazon.com/ec2/home?region=ap-northeast-2#Instances:
```

### 방법 2: AWS 메인에서 접속

1. <https://aws.amazon.com/ko/> 접속
2. 우측 상단 "콘솔에 로그인" 클릭
3. 이메일/비밀번호 입력
4. 좌측 상단 "서비스" → "EC2" 클릭
5. 좌측 메뉴 "인스턴스" 클릭

---

## 🚀 2. EC2 인스턴스 시작

1. **인스턴스 목록에서 KBO 대시보드 인스턴스 찾기**
   - 이름: kbo-dashboard 또는 유사한 이름
   - 상태: stopped (중지됨) 또는 running (실행 중)

2. **인스턴스 체크박스 선택**

3. **인스턴스 시작**
   - 방법 A: 우클릭 → "인스턴스 시작"
   - 방법 B: 상단 "인스턴스 상태" 버튼 → "인스턴스 시작"

4. **대기** (1-2분)
   - 상태가 "running"으로 변경될 때까지

5. **IP 주소 확인**
   - 퍼블릭 IPv4 주소: 43.200.4.183 (변경 여부 확인)

---

## 💻 3. PowerShell에서 EC2 접속

EC2가 "running" 상태가 되면:

```powershell
ssh -i C:\Users\김승곤\.ssh\kbo-key.pem ubuntu@43.200.4.183
```

접속 성공하면 다음 명령어 실행:

```bash
cd ~/b_project
git pull
sudo systemctl restart kbo-dashboard
exit
```

---

## 🌐 4. 대시보드 확인

브라우저에서:

```
http://43.200.4.183:8502/Player_Stats
```

투수 통계 탭 클릭 → 281명 투수 데이터 확인! 🎉

---

## 📸 스크린샷 가이드

### AWS 콘솔 화면

```
┌─────────────────────────────────────────┐
│ AWS Management Console                  │
├─────────────────────────────────────────┤
│ 서비스 ▼  리소스 그룹  계정 ▼           │
├─────────────────────────────────────────┤
│ EC2 대시보드                            │
│                                         │
│ ┌─ 리소스 ─────────────────────┐       │
│ │ 인스턴스 (실행 중)      1     │       │
│ │ 인스턴스 (중지됨)       0     │       │
│ └───────────────────────────────┘       │
│                                         │
│ ┌─ 인스턴스 ───────────────────┐       │
│ │ ☑ kbo-dashboard              │       │
│ │   상태: running              │       │
│ │   IP: 43.200.4.183           │       │
│ └───────────────────────────────┘       │
└─────────────────────────────────────────┘
```

### 인스턴스 시작 버튼 위치

```
┌─────────────────────────────────────────┐
│ [인스턴스 상태 ▼] [연결] [작업 ▼]      │
│    │                                    │
│    ├─ 인스턴스 시작  ← 여기 클릭!      │
│    ├─ 인스턴스 중지                    │
│    └─ 인스턴스 재부팅                  │
└─────────────────────────────────────────┘
```

---

## ⚠️ 문제 해결

### Q: AWS 로그인 정보를 모르겠어요

A: AWS 계정 생성 시 사용한 이메일과 비밀번호 필요

### Q: 인스턴스가 안 보여요

A: 우측 상단 리전이 "서울 (ap-northeast-2)"인지 확인

### Q: IP 주소가 바뀌었어요

A: 새 IP로 접속하거나, Elastic IP 할당 필요

---

## 🎯 빠른 체크리스트

- [ ] AWS 콘솔 접속
- [ ] EC2 → 인스턴스 메뉴
- [ ] 인스턴스 선택
- [ ] 인스턴스 시작
- [ ] 상태 "running" 확인
- [ ] PowerShell에서 SSH 접속
- [ ] git pull 실행
- [ ] 대시보드 재시작
- [ ] 브라우저에서 확인
