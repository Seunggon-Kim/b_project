# 작업 기록 (2026-01-25)

## 📊 선수 정보 크롤링 및 대시보드 추가

### 1. 선수 상세 정보 크롤러 개발

#### 파일 생성

- `data_collection/player_info_scraper.py` - 본 크롤러
- `data_collection/player_info_scraper_test.py` - 테스트 크롤러 (5명)
- `update_player_teams.py` - 팀 정보 업데이트 스크립트
- `check_players.py` - 선수 정보 확인 스크립트

#### 주요 기능

- **타자/투수 페이지 자동 감지**
  - 타자: `https://www.koreabaseball.com/Record/Player/HitterDetail/Basic.aspx?playerId={id}`
  - 투수: `https://www.koreabaseball.com/Record/Player/PitcherDetail/Basic.aspx?playerId={id}`
  - 두 페이지를 순차적으로 시도하여 자동 감지

- **수집 데이터**
  - 기본 정보: 이름, 등번호, 포지션, 투타
  - 신체 정보: 생년월일, 신장, 체중
  - 경력 정보: 입단년도, 지명순위, 경력
  - 계약 정보: 계약금, 연봉
  - 이미지: 프로필 사진 URL

- **안정성 강화**
  - 재시도 로직 (최대 3번)
  - 이미 수집된 선수 건너뛰기
  - 10명마다 자동 저장
  - 진행률 로깅 (100명마다)
  - Ctrl+C 중단 시 데이터 보존

#### 실행 결과

- **로컬**: 585명 전체 성공 (실패 0명)
- **EC2**: 585명 전체 성공 (실패 0명)
- 소요 시간: 약 30분

### 2. 데이터베이스 업데이트

#### players 테이블 스키마

```sql
CREATE TABLE IF NOT EXISTS players (
    player_id TEXT PRIMARY KEY,
    player_name TEXT NOT NULL,
    team_id TEXT,
    back_number INTEGER,
    position TEXT,
    throw TEXT CHECK(throw IN ('R', 'L', 'S')),
    bat TEXT CHECK(bat IN ('R', 'L', 'S')),
    birthday DATE,
    height INTEGER,
    weight INTEGER,
    career TEXT,
    draft_year TEXT,
    draft_order TEXT,
    signing_bonus INTEGER,
    salary INTEGER,
    image_url TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

#### 팀 정보 업데이트

- 타자/투수 통계 테이블에서 `player_team` 컬럼 참조
- 585명 전체 팀 정보 매칭 완료

**팀별 선수 수:**

- 키움: 67명
- 두산: 63명
- KIA: 61명
- 삼성: 60명
- 롯데: 59명
- KT: 58명
- NC: 57명
- SSG: 56명
- LG: 54명
- 한화: 50명

### 3. 대시보드 - Player Analytics 페이지 추가

#### 파일 변경

- `dashboard/pages/3_Game_Analysis.py` → `dashboard/pages/3_Player_Analytics.py`
- 사이드바 메뉴: "Game Analysis" → "Player Analytics"

#### 주요 기능

**1. 선수 검색**

- 이름으로 검색
- 동명이인 처리 (선택 가능)

**2. 레이아웃 (3행 구조)**

**1행: 선수 정보** (3열)

- 1열: 선수 사진
- 2열: 프로필 정보
  - 기본 정보 (포지션, 투타, 생년월일, 신체)
  - 계약 정보 (입단, 계약금, 연봉)
  - 경력
- 3열: 2025 시즌 성적
  - 타자: PA, AB, R, H, HR, AVG, OBP, SLG, OPS
  - 투수: W, L, ERA, G, GS, SV, IP, SO, WHIP

**2행: 추가 분석** (추후 확장)
**3행: 상세 통계** (추후 확장)

#### 기술적 개선

- **포지션 기반 성적 표시**: 투수는 투수 성적 우선 표시
- **컬럼명 매칭**: DB 실제 컬럼명과 일치
  - 타자: `run`, `home_run`, `on_base_plus_slugging`
  - 투수: `save`, `strikeout`, `walks_plus_hits_per_inning_pitched`
- **안타 계산**: `single + double + triple + home_run`
- **타입 에러 처리**: `innings_pitched` 문자열 처리

### 4. 배포

#### GitHub

- 모든 변경사항 커밋 및 푸시
- 커밋 메시지:
  - "Add player info crawler with team data integration"
  - "Add Player Analytics page with search and stats"
  - "Fix column names and layout improvements"

#### EC2

- 코드 pull 및 대시보드 재시작
- 선수 정보 크롤링 완료
- 팀 정보 업데이트 완료
- 대시보드 정상 작동 확인

### 5. 주요 이슈 및 해결

#### 이슈 1: 컬럼명 불일치

- **문제**: `runs`, `hits`, `home_runs` 등 컬럼이 DB에 없음
- **해결**: 실제 컬럼명 확인 및 수정
  - `runs` → `run`
  - `hits` → `single + double + triple + home_run` (계산)
  - `home_runs` → `home_run`
  - `stolen_bases` → 제거 (DB에 없음)

#### 이슈 2: 투수 통계 컬럼명

- **문제**: `saves`, `strikeouts`, `whip` 컬럼이 DB에 없음
- **해결**:
  - `saves` → `save`
  - `strikeouts` → `strikeout`
  - `whip` → `walks_plus_hits_per_inning_pitched`

#### 이슈 3: ValueError (innings_pitched)

- **문제**: `innings_pitched`가 문자열일 때 포맷팅 에러
- **해결**: try-except로 타입 처리

#### 이슈 4: 투수 성적 미표시

- **문제**: 투수 포지션인데 타자 성적 먼저 체크
- **해결**: 포지션이 "투수"면 투수 성적 우선 표시

### 6. 다음 단계 제안

1. **추가 분석 섹션 (2행)**
   - 시즌별 성적 추이 그래프
   - 팀 내 순위
   - 리그 평균 대비 비교

2. **상세 통계 섹션 (3행)**
   - 월별/상대팀별 성적
   - 홈/원정 성적
   - 최근 10경기 성적

3. **선수 비교 기능**
   - 2명 이상 선수 비교
   - 레이더 차트

4. **필터 개선**
   - 팀별 선수 목록
   - 포지션별 필터
   - 성적 순위

---

## 📈 통계

- **총 작업 시간**: 약 4시간
- **수집된 선수**: 585명
- **생성된 파일**: 4개
- **수정된 파일**: 3개
- **커밋 수**: 10+
- **해결된 이슈**: 4개

## ✅ 완료 항목

- [x] 선수 정보 크롤러 개발
- [x] 타자/투수 페이지 자동 감지
- [x] 재시도 및 에러 처리
- [x] 팀 정보 업데이트
- [x] Player Analytics 페이지 추가
- [x] 선수 검색 기능
- [x] 동명이인 처리
- [x] 시즌 성적 표시
- [x] 3행 레이아웃 구성
- [x] EC2 배포
- [x] 컬럼명 이슈 해결
- [x] 타입 에러 해결
