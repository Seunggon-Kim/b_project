# KBO 통계 사이트 Cloudflare 이전 설계

작성일: 2026-08-17
상태: 승인됨
대상 저장소: `Seunggon-Kim/b_project`

---

## 1. 배경

2026-07-14에 AWS 무료 플랜이 종료되면서 EC2 인스턴스가 정지했습니다. `https://bstats.duckdns.org/kbo/` 가 그날부터 접속되지 않습니다.

AWS 계정은 2026-10-12까지 유료 전환하지 않으면 삭제됩니다. Oracle Cloud Always Free 가입을 시도했으나 계정 생성 단계에서 두 번 실패했고 지원팀 문의 상태입니다. 한국 리전은 신규 무료 계정 목록에 없어 도쿄를 선택했으나 그것과 무관하게 막혔습니다.

**예산 0원이 절대 조건입니다.** VPS, 도메인 구입, 하드웨어 구입은 선택지에서 제외합니다.

### 복구 원본 위치

| 대상 | 위치 | 상태 |
|---|---|---|
| 코드 전체 | GitHub `origin/main` | 온전함 |
| DB `kbo_stats.db` | 로컬 `Desktop\b_project\database\` 127.5MB | 2026-06-06까지. **재크롤링 필요** |
| `wrc_plus_comparison` 테이블 | 없음 | 파크팩터 파이프라인으로 생성 |
| `player_history` 테이블 | 없음 | `registry_sync` 로 생성 |
| `player_photos` | 없음 | `mirror_player_photos.py` 로 재생성 |
| `ANTHROPIC_API_KEY` | 없음 (EC2 환경변수였음) | 신규 발급 |

---

## 2. 목표와 비목표

### 목표

- 현재 사이트의 **기능을 그대로** 되살립니다. 엔드포인트 29개, 페이지 7개 전부입니다.
- **운영 비용 0원**을 영구히 유지합니다.
- 서버 관리를 없앱니다. nginx, systemd, certbot, iptables, 배포 스크립트가 모두 불필요해집니다.

### 비목표

- 기능 추가나 화면 개편은 하지 않습니다. 이식에만 집중합니다.
- 성능 최적화는 무료 한도를 지키는 선에서만 합니다.
- `bstats.duckdns.org` 주소 유지는 포기합니다. DuckDNS가 CNAME을 지원하지 않아 Cloudflare Pages에 연결할 수 없습니다.

---

## 3. 현재 시스템 실측

| 항목 | 값 |
|---|---|
| DB 파일 크기 | 127.5 MB |
| 전체 행 수 | 232,094 |
| `play_by_play` | 229,667행 (전체의 99%) |
| 나머지 10개 테이블 | 합계 2,427행 |
| API 엔드포인트 | 29개 (`api/main.py` 1,565줄) |
| 프론트엔드 페이지 | 7개 (`dashboard_js/`) |
| 크론 작업 | 9종 |

`play_by_play` 를 제외하면 데이터가 매우 작습니다. 이 한 테이블이 설계 제약의 대부분을 만듭니다.

---

## 4. 아키텍처

```
[GitHub Actions]  Python 크롤러 9종 (Selenium 포함) 그대로 실행
       |
       |  wrangler d1 execute --remote
       v
[Cloudflare D1]   SQLite 호환 DB. 127MB, 232,094행
       ^
       |  조회
[Workers]         api/main.py 1,565줄을 JS로 이식. 엔드포인트 29개
       ^
       |
[Pages]           dashboard_js 정적 파일. API 주소만 교체
[R2]              선수 사진, 테이블 CSV 사전 생성본
```

주소는 `<프로젝트명>.pages.dev` 를 사용합니다. 무료이고 고정이며 HTTPS가 자동입니다.

### 재작성 범위

| 대상 | 처리 |
|---|---|
| `api/main.py` (1,565줄) | **JS로 이식.** 유일한 재작성 대상 |
| 크롤러 9종 (Python) | **변경 없음.** GitHub Actions에서 실행 |
| `park_factors/` 파이프라인 | **변경 없음.** Actions에서 실행 |
| `dashboard_js/` 7개 페이지 | API 베이스 URL만 교체 |
| SQL 쿼리문 | **변경 없음.** D1이 SQLite 호환 |
| `deploy.sh`, `auto_deploy_poll.sh` | **삭제.** Pages가 push 감지해 자동 배포 |
| nginx, systemd, certbot, iptables | **삭제.** 불필요 |

### 왜 무거운 계산을 Worker에서 하지 않는가

Workers 무료 플랜은 호출당 CPU가 10ms입니다. 그러나 현재 구조가 이미 이 제약에 맞습니다. 파크팩터와 wRC+는 크론이 미리 계산해 `wrc_plus_comparison` 테이블에 적재하고, API는 그것을 읽기만 합니다. 무거운 계산은 Actions에 남기고 Worker는 조회와 직렬화만 담당합니다.

### API를 Pages Functions가 아닌 별도 Worker로 두는 이유

Pages의 정적 파일 서빙은 대역폭 제한이 없습니다. API를 Pages Functions로 만들면 정적 요청까지 Workers 일일 10만 요청 한도에 포함될 수 있습니다. API를 독립 Worker로 분리하면 정적 서빙이 한도를 소비하지 않습니다.

---

## 5. 데이터 파이프라인

### 작업 배치

| 작업 | 현재 크론 (KST) | 새 위치 | 근거 |
|---|---|---|---|
| official_stats (Selenium) | 매일 02:00 | Actions | Selenium은 Workers에서 불가 |
| pbp + games 보강 | 매일 03:30 | Actions | pandas 의존 |
| player_detector | 매일 04:00 | Actions | |
| cleanup | 매일 04:30 | Actions | |
| park_factors | 매일 05:00 | Actions | 계산이 무거움 |
| registry_sync | 매일 05:40 | Actions | |
| player_info | 매월 1일 13:00 | Actions | |
| futures | 30분 간격 12:00~23:30 | **Worker Cron** | 가볍고 빈도가 높음 |
| auto_deploy | 3분마다 | **삭제** | Pages 자동 배포로 대체 |
| gen_cron_status | 15분마다 | **삭제** | Actions가 실행 시각을 D1에 직접 기록 |

### 워크플로 구성

현재 크론 9개는 시각을 나눠 돌지만 실제로는 의존 체인입니다. pbp가 끝나야 games 보강이 되고, 그래야 park_factors가 정확합니다. 하나로 묶습니다.

```
daily.yml    매일 1회 순차 실행
             official_stats -> pbp -> games 보강 -> player_detector
             -> cleanup -> registry_sync -> park_factors
             -> D1 적재 -> R2 CSV 갱신 -> 실행 시각 기록

monthly.yml  매월 1일: player_info
```

각 단계는 독립적으로 실패할 수 있게 합니다. `official_stats` 가 실패해도 `pbp` 는 진행합니다.

### GitHub Actions 분 예산

저장소가 비공개이므로 월 2,000분이 무료 한도입니다.

```
daily.yml     하루 20~30분 x 30일 = 월 600~900분
monthly.yml   월 30분
합계          월 630~930분   (한도 2,000분)
```

futures를 Actions에 두면 30분 간격 기준 월 720분이 추가되어 예산이 빠듯해집니다. 그래서 Worker Cron으로 분리했습니다.

### D1 적재 방식

Actions가 `wrangler d1 execute --remote` 로 SQL을 전송합니다. Cloudflare API 토큰은 GitHub Secrets에 둡니다.

일일 쓰기량 추정입니다.

```
PBP           경기 5개 x 약 300플레이 = 약 1,500행
공식 기록     타자 398 + 투수 281 upsert = 약 680행
파생 지표     수백 행
합계          약 3,000행   (한도 100,000행/일)
```

### 최초 적재는 3일에 나눕니다

D1 무료 쓰기 한도가 하루 10만 행인데 `play_by_play` 만 229,667행입니다.

```
1일차   play_by_play 외 전체 (2,427행) + PBP 약 90,000행
2일차   PBP 약 90,000행
3일차   PBP 나머지 약 50,000행
```

일회성 작업입니다. 이 3일 동안은 사이트를 공개하지 않습니다.

### 인덱스

D1 읽기 한도는 하루 500만 행입니다. `play_by_play` 전체 스캔이 한 번에 229,667행을 소비하므로 인덱스가 필수입니다.

```sql
CREATE INDEX idx_pbp_date    ON play_by_play(game_date);
CREATE INDEX idx_pbp_pitcher ON play_by_play(pitcher_ID, game_date);
CREATE INDEX idx_pbp_batter  ON play_by_play(batter_ID, game_date);
CREATE INDEX idx_pbp_game    ON play_by_play(gameID);
```

이식 과정에서 실제 쿼리 패턴을 보고 인덱스를 추가합니다.

### CSV 전체 내보내기

`/db/table/{table_name}/csv?limit=0` 은 테이블 전체를 반환합니다. `play_by_play` 요청 시 Worker의 CPU 10ms, 요청당 D1 쿼리 50개, 메모리 128MB를 모두 초과합니다.

Actions가 매일 테이블별 CSV를 생성해 gzip 압축 후 R2에 올려둡니다. Worker는 R2 링크로 리다이렉트합니다.

| 요청 | 처리 |
|---|---|
| `?limit=0` (전체) | R2 사전 생성 파일로 302 리다이렉트 |
| `?limit=N` (부분) | Worker가 즉석 생성 |
| 작은 테이블 (`players` 585행 등) | Worker가 즉석 생성 |

프론트엔드와 URL은 변경하지 않습니다. R2는 저장 10GB 무료이고 전송료가 없습니다. `play_by_play` CSV는 압축 시 10MB 안팎으로 예상합니다.

현재 EC2는 요청마다 22만 행을 즉석 생성합니다. nginx 타임아웃이 180초로 설정된 것이 그 부담을 보여줍니다. 사전 생성 방식이 더 빠릅니다.

**동작 차이 한 가지**: R2 파일은 마지막 일일 갱신 시점 기준입니다. 다만 이 데이터는 하루 한 번만 변경되므로 실질적 차이가 없습니다.

---

## 6. 무료 한도 대비 사용량

| 자원 | 무료 한도 | 예상 사용 | 여유 |
|---|---|---|---|
| Workers 요청 | 100,000/일 | 페이지뷰 1만/일까지 감당 | 충분 |
| Workers CPU | 10ms/호출 | 조회·직렬화 2~3ms | 충분 |
| Workers Cron | 5개/계정 | 1개 (futures) | 충분 |
| Workers 메모리 | 128MB | 소량 | 충분 |
| 요청당 subrequest | 50개 | 외부 호출 1~2개 | 충분 |
| **D1 저장** | **DB당 500MB** | 127MB, 연 +120MB | **약 3년 후 한계** |
| D1 총 저장 | 5GB, DB 10개 | 1개 사용 | 충분 |
| D1 읽기 | 5,000,000행/일 | 인덱스 적용 시 소량 | 충분 |
| D1 쓰기 | 100,000행/일 | 약 3,000행 | 충분 |
| D1 쿼리/호출 | 50개 | 엔드포인트당 1~5개 | 충분 |
| Pages 빌드 | 500회/월 | push 횟수 | 충분 |
| Pages 대역폭 | 무제한 | | 충분 |
| R2 저장 | 10GB | 100MB 미만 | 충분 |
| R2 전송 | 무료 | | 충분 |
| GitHub Actions | 2,000분/월 | 630~930분 | 충분 |

### 3년 후 저장 한계 대응

`play_by_play` 는 시즌당 약 216,000행, 약 120MB씩 증가합니다. 현재 127MB에서 시작해 약 3년 후 DB당 500MB 한도에 도달합니다.

무료 플랜에서 DB를 10개까지 만들 수 있으므로 그때 시즌 단위로 분리하면 총 5GB까지 확장됩니다. 지금 설계에 반영하지 않고 기록만 남깁니다.

---

## 7. 위험과 대응

### 위험 1. KBO 사이트가 GitHub Actions를 차단할 수 있습니다

현재 Selenium 크롤러는 서울 EC2에서 실행됩니다. GitHub Actions 러너는 해외 데이터센터 IP입니다. 국내 사이트가 해외 데이터센터 트래픽을 차단하는 사례가 흔합니다.

발생 시 매일 02:00 공식 기록 수집이 실패해 타자·투수 성적이 갱신되지 않습니다.

**대응**: `official_stats` 만 집 PC의 작업 스케줄러로 돌리고 결과를 D1에 전송합니다. PC를 24시간 켜둘 필요는 없으며 하루 한 번 켜져 있을 때 실행되면 됩니다. 나머지 설계는 그대로 유지됩니다.

**확인 시점**: M1에서 DB 재구축을 Actions로 실행하며 판명됩니다.

### 위험 2. 네이버 스포츠가 Cloudflare 엣지를 차단할 수 있습니다

실시간 경기 데이터를 `api-gw.sports.naver.com` 에서 가져옵니다. 현재는 서울 EC2가 호출하지만 Workers는 전 세계 엣지에서 호출합니다. 국가나 IP 기준으로 차단될 수 있습니다.

발생 시 실시간 경기 화면만 동작하지 않습니다. 기록 조회는 정상입니다.

**대응**: 우회 수단이 마땅치 않습니다. 실시간 기능을 포기하거나 해당 부분만 집 PC에서 중계하는 구조가 됩니다. **이 설계에서 기능 손실이 발생할 수 있는 유일한 지점입니다.**

**확인 시점**: M3에서 네이버 연동 엔드포인트를 먼저 이식하며 판명됩니다.

### 위험 3. futures를 Worker Cron에서 처리하지 못할 수 있습니다

CPU 10ms 안에 KBO 퓨처스 일정 파싱이 끝난다는 가정에 기반합니다. 하루 5경기 남짓이라 가능할 것으로 보지만 실측 전에는 확정할 수 없습니다.

**대응**: Actions로 옮기고 주기를 2시간으로 늘립니다. 월 360분이 추가되어 예산 안에 들어옵니다.

### 위험 4. 이식 과정의 계산 오차

1,565줄을 다른 언어로 옮기면서 숫자가 미세하게 달라질 수 있습니다. 특히 나눗셈, 반올림, 정렬 순서, NULL 처리입니다.

**대응**: 9장의 골든 응답 비교로 기계적으로 검증합니다.

---

## 8. 비밀 관리

2026-07-05에 공개 저장소에 커밋된 Gmail 앱 비밀번호가 도용되어 계정에서 대량 피싱 메일이 발송된 사고가 있었습니다. 해당 자격증명은 폐기되었고 저장소는 비공개로 전환했습니다.

**git에는 어떤 비밀도 두지 않습니다.**

| 비밀 | 보관 위치 | 사용처 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Workers 시크릿 (`wrangler secret put`) | AI 프록시 엔드포인트 |
| Cloudflare API 토큰 | GitHub Secrets | Actions에서 D1 적재 |
| Gmail 앱 비밀번호 | GitHub Secrets | 수집 완료 알림 메일 |

`config/email_config.json` 은 커밋 `3aa6fbb` 에서 추적 해제하고 `.gitignore` 에 등록했습니다. Actions에서는 Secrets를 환경변수로 주입합니다.

---

## 9. 오류 처리

| 상황 | 처리 |
|---|---|
| Actions 단계 실패 | GitHub이 실패 알림 메일 발송. 단계별 독립 실행이라 후속 단계는 진행 |
| Worker에서 D1 오류 | 500 응답과 로깅. 현재 `global_exception_handler` 와 동등하게 구현 |
| 네이버 호출 실패 | 화면에 "일시적으로 불러올 수 없습니다" 표시. 나머지 화면은 정상 동작 |
| D1 일일 한도 소진 | 안내 메시지 표시. 인덱스가 적절하면 발생하지 않아야 함 |
| R2 CSV 파일 부재 | 부분 조회로 대체하고 안내 표시 |

---

## 10. 테스트 전략: 골든 응답 비교

이식 작업의 성공 기준은 **같은 요청에 같은 응답**입니다. 화면을 눈으로 확인하는 대신 기계적으로 검증합니다.

```
1. 로컬에서 현재 FastAPI를 실행합니다 (로컬 SQLite 사용)
2. 29개 엔드포인트를 아래 파라미터 규칙으로 호출해 응답 JSON을 저장합니다  <- 정답지
3. 이식한 Worker에 동일한 요청을 보냅니다
4. 응답을 정답지와 비교합니다
```

전제 조건은 로컬 DB와 D1의 데이터가 동일해야 한다는 것입니다. M2의 최초 적재 직후에 정답지를 생성합니다.

### 파라미터 조합 규칙

엔드포인트마다 아래 경우를 모두 생성합니다.

- **파라미터 없음**: 기본값 동작 확인
- **경로 파라미터**: 실제 존재하는 값 3개(국내 타자, 국내 투수, 외국인 선수 각 1명)와 존재하지 않는 값 1개
- **시즌 파라미터**: 데이터가 있는 시즌 2개와 데이터가 없는 시즌 1개
- **경계값**: `limit=1`, `min_pa=0`, `min_pa=999`(결과 0건), 날짜 범위의 첫날과 마지막날
- **정렬·방향 파라미터**: 지원하는 값 전부

결과 0건과 존재하지 않는 ID는 반드시 포함합니다. 이식 과정에서 NULL과 빈 배열 처리가 어긋나는 일이 가장 흔합니다.

### 비교 규칙

- **부동소수점**: 상대 오차 `1e-9` 이내면 같다고 봅니다. 화면에 표시되는 지표는 소수점 3자리까지가 대부분이라 이 기준이면 충분합니다
- **정수·문자열**: 완전 일치를 요구합니다. 특히 `player_id` 는 문자열과 정수 혼용 이력이 있어 타입까지 비교합니다
- **배열 순서**: `ORDER BY` 가 명시된 쿼리는 순서까지 비교합니다. 명시되지 않은 쿼리는 정렬 후 비교합니다
- **NULL**: `null`, 빈 문자열, `0` 을 구분해 비교합니다

---

## 11. 마일스톤

위험한 작업을 앞에 배치했습니다. 별도 검증 단계를 두지 않고, 실제 작업 안에서 위험이 조기에 드러나게 합니다.

| 단계 | 내용 | 판명되는 것 |
|---|---|---|
| **M1** | Cloudflare 계정 생성, D1 데이터베이스 생성. 로컬 DB 재구축(2026-06-06 이후 재크롤링, 누락 테이블 2개 생성)을 **GitHub Actions에서 실행** | **위험 1** |
| **M2** | D1 3일 분할 적재, 인덱스 생성, 골든 응답 정답지 생성 | |
| **M3** | API 이식. **네이버 연동 5개(`schedule`, `schedule/futures`, `standings`, `leaders`, `players/{id}/news`)부터** | **위험 2** |
| **M4** | 나머지 24개 엔드포인트 이식, 골든 비교 통과 | |
| **M5** | 프론트엔드 API 베이스 URL 교체, Pages 배포 | |
| **M6** | Actions 워크플로 정기화, Worker Cron 등록, R2 CSV 내보내기 | **위험 3** |
| **M7** | 공개, 모니터링 | |

M1에서 DB 재구축을 Actions로 실행하는 이유는, 어차피 필요한 작업이므로 이를 통해 위험 1이 첫 단계에서 드러나기 때문입니다. 차단되면 로컬로 전환하며, 버려지는 작업은 없습니다.

M4가 가장 깁니다. wRC+ 계열 8개와 DB 탐색 3개가 무거운 편입니다.

---

## 12. AWS 정리

별도 조치가 필요 없습니다. 2026-10-12에 계정이 자동 종료되며 리소스가 삭제됩니다. 복원할 항목은 1장 표로 확인되었습니다.

컷오버 전에 AWS 콘솔에 로그인해 EC2 외에 다른 리소스가 있는지 육안으로 확인하는 것을 권합니다. 확인 자체는 무료입니다.

---

## 13. 이 설계로 없어지는 것

| 항목 | 사유 |
|---|---|
| `deploy.sh` | Pages가 push를 감지해 자동 배포 |
| `auto_deploy_poll.sh` | 위와 동일 |
| `kbo-api.service` (systemd) | Workers가 실행 관리 |
| `kbo-dashboard.conf` (nginx) | Pages가 정적 서빙과 라우팅 담당 |
| certbot, Let's Encrypt 갱신 | Pages가 HTTPS 자동 처리 |
| iptables, 보안목록 | 서버가 없음 |
| DuckDNS 갱신 | 고정 주소 `pages.dev` |
| 서버 SSH 접속 | 서버가 없음 |
