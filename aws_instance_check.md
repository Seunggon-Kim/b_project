# AWS EC2 인스턴스 확인 가이드

어제 만든 EC2 인스턴스를 확인하고 IP 주소를 얻는 방법입니다.

---

## 🌐 1단계: AWS 콘솔 접속

### 방법 A: 직접 링크로 접속 (빠름) ⚡

**EC2 인스턴스 페이지 바로가기:**

```
https://console.aws.amazon.com/ec2/home#Instances:
```

위 링크를 브라우저에 복사하여 접속하세요.

### 방법 B: AWS 홈페이지에서 접속

1. **AWS 콘솔 접속**
   - <https://aws.amazon.com/ko/> 접속
   - 우측 상단 **"콘솔에 로그인"** 클릭

2. **로그인**
   - **루트 사용자** 선택
   - 이메일 주소 입력
   - 비밀번호 입력
   - **로그인** 클릭

3. **EC2 서비스로 이동**
   - 상단 검색창에 **"EC2"** 입력
   - **EC2** 클릭

---

## 📋 2단계: 인스턴스 목록 확인

로그인 후 EC2 대시보드에서:

1. **좌측 메뉴에서 "인스턴스" 클릭**
   - 또는 영어: "Instances" 클릭

2. **인스턴스 목록 확인**
   - 어제 만든 인스턴스가 보일 것입니다
   - 인스턴스 이름: **`kbo-stats-server`** (가이드대로 만들었다면)

---

## 🔍 3단계: 인스턴스 상태 확인

인스턴스 목록에서 확인할 사항:

### ✅ 인스턴스 상태 (Instance State)

| 상태 | 의미 | 조치 |
|------|------|------|
| 🟢 **실행 중** (Running) | 정상 작동 중 | ✅ 크롤링 가능 |
| 🟡 **중지됨** (Stopped) | 꺼져 있음 | ⚠️ 시작 필요 |
| 🔴 **종료됨** (Terminated) | 삭제됨 | ❌ 재생성 필요 |

### 📊 확인할 정보

인스턴스를 클릭하면 하단에 상세 정보가 표시됩니다:

```
✅ 인스턴스 ID: i-0123456789abcdef0
✅ 인스턴스 유형: t3.micro
✅ 퍼블릭 IPv4 주소: EC2_PUBLIC_IP ← 이것이 필요!
✅ 프라이빗 IPv4 주소: 172.31.x.x
✅ 가용 영역: ap-northeast-2a
✅ 상태: 실행 중
```

---

## 📝 4단계: IP 주소 복사

### 퍼블릭 IP 주소 찾기

1. **인스턴스 클릭** (체크박스가 아닌 이름 클릭)
2. 하단 **"세부 정보"** 탭에서 찾기:
   - **퍼블릭 IPv4 주소** 또는 **Public IPv4 address**
   - 예: `EC2_PUBLIC_IP`

3. **IP 주소 복사**
   - IP 주소 옆의 복사 아이콘 클릭
   - 또는 마우스로 드래그하여 복사 (Ctrl+C)

---

## 🎯 5단계: IP 주소로 상태 확인

IP 주소를 복사했다면, PowerShell에서 다음 명령어 실행:

```powershell
cd C:\Users\USERNAME\Desktop\b_project
.\check_aws_status.ps1 [복사한_IP_주소]
```

**예시:**

```powershell
.\check_aws_status.ps1 EC2_PUBLIC_IP
```

---

## 🚨 인스턴스가 중지된 경우

### 인스턴스 시작하기

1. **인스턴스 선택** (체크박스 클릭)
2. 상단 **"인스턴스 상태"** 버튼 클릭
3. **"인스턴스 시작"** 클릭
4. 상태가 **"실행 중"**으로 바뀔 때까지 대기 (1-2분)

⚠️ **주의:** 인스턴스를 시작하면 **퍼블릭 IP 주소가 변경**될 수 있습니다!

- 새로운 IP 주소를 다시 확인하세요

---

## 📸 스크린샷 참고

### EC2 대시보드 화면 예시

```
┌─────────────────────────────────────────────────────────┐
│ EC2 대시보드                                              │
├─────────────────────────────────────────────────────────┤
│ 좌측 메뉴:                                                │
│   ├─ 인스턴스                    ← 여기 클릭!             │
│   ├─ 이미지                                               │
│   ├─ Elastic Block Store                                │
│   └─ ...                                                 │
└─────────────────────────────────────────────────────────┘
```

### 인스턴스 목록 화면 예시

```
┌──────────────────────────────────────────────────────────────────┐
│ 인스턴스                                                          │
├──────────────────────────────────────────────────────────────────┤
│ ☑ 이름              인스턴스 ID    상태      퍼블릭 IPv4 주소    │
│ ☐ kbo-stats-server  i-0abc123...  실행 중   EC2_PUBLIC_IP       │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔧 문제 해결

### 문제 1: 인스턴스가 보이지 않음

**확인 사항:**

1. **리전(Region) 확인**
   - 우측 상단에서 리전 확인
   - **서울 (ap-northeast-2)** 선택되어 있는지 확인
   - 다른 리전에 생성했다면 해당 리전으로 변경

2. **필터 확인**
   - 인스턴스 목록 상단의 필터 제거
   - "모든 인스턴스" 또는 "All instances" 선택

### 문제 2: 퍼블릭 IP가 없음

**원인:**

- 인스턴스가 중지되었다가 다시 시작됨
- 퍼블릭 IP가 자동 할당되지 않음

**해결:**

- Elastic IP 할당 (고정 IP)
- 또는 인스턴스 재시작 후 새 IP 확인

### 문제 3: 인스턴스가 종료됨 (Terminated)

**원인:**

- 실수로 삭제됨
- 자동 종료 설정

**해결:**

- 새 인스턴스 생성 필요
- `AWS_AUTOMATION_GUIDE.md` 참고하여 재생성

---

## 📞 다음 단계

IP 주소를 확인했다면:

1. **상태 확인 스크립트 실행**

   ```powershell
   .\check_aws_status.ps1 [IP주소]
   ```

2. **문제가 있다면**
   - `check_aws_logs.md` 참고
   - 수동 크롤링 실행

3. **SSH 직접 접속**

   ```powershell
   ssh -i $HOME\.ssh\kbo-key.pem ubuntu@[IP주소]
   ```

---

**작성일:** 2026-01-15
**목적:** 어제 만든 EC2 인스턴스 확인 및 IP 주소 획득
